{
  "name": "@xoai/medscab",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@xoai/db": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@xoai/typescript-config": "workspace:*",
    "typescript": "^5.3.3",
    "vitest": "^1.6.0"
  }
}
